export class Weapon {

    width: number = 200;
    height: number = 200;

    constructor() {}

    // Create the items for the game.
    create(): void {
    }

    // Update the game based on logic or input.
    update(): void {
    }
};